# DEMO

## 启动

```bash
cd starter
python -m uvicorn kbqa.server:app --host 127.0.0.1 --port 8000
```

打开 <http://127.0.0.1:8000/>。

## 1. 看板筛选（约 20 秒）

1. 默认显示最近数据区间，观察净营业额、有效订单、客单价和销量四个指标。
2. 修改开始/结束日期，或选择 `S02 · Makai Poke`；趋势、Top 10 和汇总数字会一起刷新。
3. 下方“数据质量”展示保留率、原始行数和每类剔除原因。

## 2. 混合问答（约 40 秒）

点击“问问数据”，输入：

> 618 当天 S02 的牛肉poke 卖了多少份，达到目标了吗？

回答会同时使用销售数据和知识库文档。回答下方点击“查看本轮 Trace”，可看到 planner、检索命中、工具调用、最终回答阶段和每步耗时。

## 3. 调试面板（约 20 秒）

Trace 面板可从对话框按钮或页面底部“打开最近 Trace”进入。错误请求会显示在红色错误块中；没有 Trace 时按钮保持禁用，避免打开空面板。

## 演示要点

- 页面首屏即可完成日期/门店筛选，不需要进入设置页。
- 无 Key 时使用 Mock 模式，页面仍可演示完整看板和问答链路。
- 配置 Live LLM 后，Trace 会额外显示模型请求摘要和原始输出摘要。
